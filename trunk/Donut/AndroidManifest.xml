<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.android.todo" android:versionName="4.2.5"
	android:versionCode="67">
	<application android:icon="@drawable/icon" android:label="@string/app_name"
		android:theme="@android:style/Theme.NoTitleBar">
		<activity android:name=".TagToDoList" android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".Edit" android:windowSoftInputMode="stateVisible|adjustPan">
		</activity>
		<activity android:name=".Confirmation"></activity>
		<activity android:name=".Photo"></activity>
		<activity android:name=".Graphics"></activity>
		<activity android:name=".Audio"></activity>
		<activity android:name=".Alarm" android:screenOrientation="portrait"></activity>
		<activity android:name=".NotificationActivity"></activity>
		<activity android:name=".Config"></activity>
		<uses-permission android:name="android.permission.RECORD_AUDIO"></uses-permission>

		<receiver android:label="@string/full_name" android:name=".widget.TagToDoWidget">
			<intent-filter>
				<action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
			</intent-filter>
			<meta-data android:name="android.appwidget.provider"
				android:resource="@xml/widget" />
		</receiver>

		<receiver android:name=".receivers.AlarmReceiver"
			android:enabled="true"></receiver>
		<receiver android:name=".receivers.BootReceiver"
			android:enabled="true">
			<intent-filter>
				<action android:name="android.intent.action.BOOT_COMPLETED" />
			</intent-filter>
		</receiver>

		<receiver android:name=".widget.WidgetChange"></receiver>

		<activity android:name=".sync.GlobalSearchHandler"
			android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.SEARCH" />
				<category android:name="android.intent.category.DEFAULT" />
			</intent-filter>

			<meta-data android:name="android.app.searchable"
				android:resource="@xml/searchable" />
		</activity>

		<!-- Provides search suggestions for words and their definitions. -->
		<provider android:name=".sync.TaskProvider"
			android:authorities="com.android.todo.sync.provider"
			android:syncable="false" />

		<!-- The application's publisher ID assigned by AdMob -->
		<meta-data android:value="a14c5fdaad083bc" android:name="ADMOB_PUBLISHER_ID" />

		<!-- Track Market installs from AdMob ads -->
		<receiver android:name="com.admob.android.ads.analytics.InstallReceiver"
			android:exported="true">
			<intent-filter>
				<action android:name="com.android.vending.INSTALL_REFERRER" />
			</intent-filter>
		</receiver>

	</application>

	<uses-permission android:name="android.permission.RECORD_AUDIO"></uses-permission>
	<uses-permission android:name="android.permission.INTERNET"></uses-permission>
	<uses-permission android:name="android.permission.VIBRATE"></uses-permission>
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"></uses-permission>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
	<uses-permission android:name="android.permission.DISABLE_KEYGUARD"></uses-permission>
	<uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>

	<uses-sdk android:minSdkVersion="4"></uses-sdk>

</manifest> 